package test

import (
	// want "The golang.org/x/exp/constraints package import is no longer necessary"

	"cmp"
)

type _ interface {
	cmp.Ordered // want "golang.org/x/exp/constraints.Ordered can be replaced with cmp.Ordered"
}

func _[T cmp.Ordered](v T) {} // want "golang.org/x/exp/constraints.Ordered can be replaced with cmp.Ordered"
